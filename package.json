{
  "name": "aws-circleci-template",
  "version": "1.0.0",
  "description": "Template for AWS Infrastructure and CircleCi",
  "main": "index.js",
  "scripts": {
    "deploy": "yarn && NODE_ENV=production node scripts/deploy.js",
    "deploy:local": "yarn && NODE_ENV=development node scripts/deploy.js",
    "test": "yarn && NODE_ENV=development node scripts/testTemplates.js"
  },
  "author": "Grady Rogers",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "2.322.x",
    "dotenv": "6.0.x",
    "yamljs": "^0.3.0"
  }
}
